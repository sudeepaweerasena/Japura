import React from 'react';
import './SeparatorComponent.css'; // Ensure this points to the correct CSS file

function SeparatorComponent() {
  return (
    <div className="separator">
      <h1>Approval Requesting Form</h1>
    </div>
  );
}

export default SeparatorComponent;
